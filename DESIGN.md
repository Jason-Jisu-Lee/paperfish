# RAIN

rules: pure #000. thin falling lines, never drops. uniform field. zero chrome. vanilla js, no deps.

## line styles (compare.html, one enlarged line each, synced fall)
1 solid  flat alpha
2 taper  fades to tail
3 blur   fades both ends
4 comet  taper + bright head
5 soft   feathered edges

## plan
- lock winning line, field adopts it
- depth = future upgrade on fine (parallax layers), not a style
- mist storm glow rebuilt on locked line later
